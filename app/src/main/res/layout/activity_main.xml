<?xml version="1.0" encoding="utf-8"?>
<!--
    Root layout for MainActivity.
    DrawerLayout wraps:
      - Content pane (FrameLayout + BreadcrumbBar + action row)
      - Navigation Drawer (LinearLayout with sectioned ListView)

    All dimensions in dp so the layout scales correctly on
    BB10 Passport (1440×1440) and Classic (720×720).
-->
<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_primary">

    <!-- ══ Main content area ═════════════════════════════════════════════════ -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Action bar row: menu button + breadcrumbs + sort + hidden toggle -->
        <RelativeLayout
            android:id="@+id/action_bar_row"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:background="@color/toolbar_bg"
            android:paddingLeft="8dp"
            android:paddingRight="8dp">

            <!-- Drawer open button -->
            <ImageButton
                android:id="@+id/btn_drawer_open"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_centerVertical="true"
                android:layout_alignParentLeft="true"
                android:src="@drawable/ic_menu"
                android:background="?android:attr/selectableItemBackground"
                android:contentDescription="Open drawer"
                android:focusable="true" />

            <!-- Sort button -->
            <ImageButton
                android:id="@+id/btn_sort"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_centerVertical="true"
                android:layout_alignParentRight="true"
                android:src="@drawable/ic_sort"
                android:background="?android:attr/selectableItemBackground"
                android:contentDescription="Sort options"
                android:focusable="true" />

            <!-- Toggle hidden files -->
            <ImageButton
                android:id="@+id/btn_toggle_hidden"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_centerVertical="true"
                android:layout_toLeftOf="@id/btn_sort"
                android:src="@drawable/ic_eye"
                android:background="?android:attr/selectableItemBackground"
                android:contentDescription="Toggle hidden files"
                android:focusable="true" />

            <!-- Breadcrumb bar fills remaining space -->
            <com.blacfiles.filemanager.ui.BreadcrumbBar
                android:id="@+id/breadcrumb_bar"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_toRightOf="@id/btn_drawer_open"
                android:layout_toLeftOf="@id/btn_toggle_hidden" />
        </RelativeLayout>

        <!-- File list fragment container -->
        <FrameLayout
            android:id="@+id/content_frame"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />

    </LinearLayout>

    <!-- ══ Navigation Drawer ════════════════════════════════════════════════ -->
    <LinearLayout
        android:id="@+id/drawer_container"
        android:layout_width="260dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:orientation="vertical"
        android:background="@color/drawer_bg"
        android:focusable="true">

        <!-- Drawer header -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:gravity="center_vertical"
            android:paddingLeft="16dp"
            android:text="BlacFiles"
            android:textColor="@color/text_primary"
            android:textSize="18sp"
            android:textStyle="bold"
            android:background="@color/drawer_header_bg" />

        <!-- Sectioned drawer list -->
        <ListView
            android:id="@+id/drawer_list"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:divider="@null"
            android:focusable="true"
            android:choiceMode="singleChoice" />

    </LinearLayout>

</android.support.v4.widget.DrawerLayout>
